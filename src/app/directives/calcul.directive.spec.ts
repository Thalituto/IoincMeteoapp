import { CalculDirective } from './calcul.directive';

describe('CalculDirective', () => {
  it('should create an instance', () => {
    const directive = new CalculDirective();
    expect(directive).toBeTruthy();
  });
});
